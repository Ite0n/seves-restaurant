diff --git a/node_modules/@react-three/fiber/dist/events-583399dd.cjs.prod.js b/node_modules/@react-three/fiber/dist/events-583399dd.cjs.prod.js
index 3376d70..3167d37 100644
--- a/node_modules/@react-three/fiber/dist/events-583399dd.cjs.prod.js
+++ b/node_modules/@react-three/fiber/dist/events-583399dd.cjs.prod.js
@@ -1017,6 +1017,45 @@ const createStore = (invalidate, advance) => {
         current
       }
     }));
+    function createFiberClock(autoStart = true) {
+      const clock = {
+        autoStart,
+        startTime: 0,
+        oldTime: 0,
+        elapsedTime: 0,
+        running: false,
+        start() {
+          this.startTime = performance.now();
+          this.oldTime = this.startTime;
+          this.elapsedTime = 0;
+          this.running = true;
+        },
+        stop() {
+          this.getElapsedTime();
+          this.running = false;
+          this.autoStart = false;
+        },
+        getElapsedTime() {
+          this.getDelta();
+          return this.elapsedTime;
+        },
+        getDelta() {
+          let diff = 0;
+          if (this.autoStart && !this.running) {
+            this.start();
+            return 0;
+          }
+          if (this.running) {
+            const newTime = performance.now();
+            diff = (newTime - this.oldTime) / 1000;
+            this.oldTime = newTime;
+            this.elapsedTime += diff;
+          }
+          return diff;
+        },
+      };
+      return clock;
+    }
     const pointer = new THREE__namespace.Vector2();
     const rootState = {
       set,
@@ -1038,7 +1077,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: createFiberClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js b/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js
index 97d8295..57577a2 100644
--- a/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js
+++ b/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js
@@ -992,6 +992,45 @@ const createStore = (invalidate, advance) => {
         current
       }
     }));
+    function createFiberClock(autoStart = true) {
+      const clock = {
+        autoStart,
+        startTime: 0,
+        oldTime: 0,
+        elapsedTime: 0,
+        running: false,
+        start() {
+          this.startTime = performance.now();
+          this.oldTime = this.startTime;
+          this.elapsedTime = 0;
+          this.running = true;
+        },
+        stop() {
+          this.getElapsedTime();
+          this.running = false;
+          this.autoStart = false;
+        },
+        getElapsedTime() {
+          this.getDelta();
+          return this.elapsedTime;
+        },
+        getDelta() {
+          let diff = 0;
+          if (this.autoStart && !this.running) {
+            this.start();
+            return 0;
+          }
+          if (this.running) {
+            const newTime = performance.now();
+            diff = (newTime - this.oldTime) / 1000;
+            this.oldTime = newTime;
+            this.elapsedTime += diff;
+          }
+          return diff;
+        },
+      };
+      return clock;
+    }
     const pointer = new THREE.Vector2();
     const rootState = {
       set,
@@ -1013,7 +1052,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE.Clock(),
+      clock: createFiberClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/node_modules/@react-three/fiber/dist/events-f19bcc32.cjs.dev.js b/node_modules/@react-three/fiber/dist/events-f19bcc32.cjs.dev.js
index 1b04ea6..cf69098 100644
--- a/node_modules/@react-three/fiber/dist/events-f19bcc32.cjs.dev.js
+++ b/node_modules/@react-three/fiber/dist/events-f19bcc32.cjs.dev.js
@@ -1017,6 +1017,45 @@ const createStore = (invalidate, advance) => {
         current
       }
     }));
+    function createFiberClock(autoStart = true) {
+      const clock = {
+        autoStart,
+        startTime: 0,
+        oldTime: 0,
+        elapsedTime: 0,
+        running: false,
+        start() {
+          this.startTime = performance.now();
+          this.oldTime = this.startTime;
+          this.elapsedTime = 0;
+          this.running = true;
+        },
+        stop() {
+          this.getElapsedTime();
+          this.running = false;
+          this.autoStart = false;
+        },
+        getElapsedTime() {
+          this.getDelta();
+          return this.elapsedTime;
+        },
+        getDelta() {
+          let diff = 0;
+          if (this.autoStart && !this.running) {
+            this.start();
+            return 0;
+          }
+          if (this.running) {
+            const newTime = performance.now();
+            diff = (newTime - this.oldTime) / 1000;
+            this.oldTime = newTime;
+            this.elapsedTime += diff;
+          }
+          return diff;
+        },
+      };
+      return clock;
+    }
     const pointer = new THREE__namespace.Vector2();
     const rootState = {
       set,
@@ -1038,7 +1077,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: createFiberClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
